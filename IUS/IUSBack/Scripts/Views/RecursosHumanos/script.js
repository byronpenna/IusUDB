$(document).ready(function () {
    // plugins 
        // chosen
            $(".cbPais").chosen({ no_results_text: "Pais no encontrado", width: '100%' });
            $(".cbActividad").chosen({ no_results_text: "No se encuentra actividad", width: '100%' });
            $(".cbCargo").chosen({ no_results_text: "No se encuentra actividad", width: '100%' });
})